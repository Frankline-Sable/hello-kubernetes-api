apiVersion: v1
kind: Pod
metadata:
  name: security-demo
spec:
  containers:
    - name: security-demo
      image: nginx:alpine
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000